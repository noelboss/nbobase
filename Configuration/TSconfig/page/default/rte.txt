RTE.default {  
	contentCSS = EXT:nxbase/Resources/Public/css/bootstrap/bootstrap.min.css
	useCSS = 1
	
	# zeigt alle CSS-Klassen die in formate.css vorhanden sind
	showTagFreeClasses = 1

	classesParagraph =lead,alert
	classesAnchor = label,btn
	
	## Default RTE processing rules
	proc {
		allowedClasses = lead,alert,btn,label
		keepPDIVattribs = data, id, title, dir, lang, xml:lang, itemscope, itemtype, itemprop
		denyTags ( 
			h2, u,img,center,font,hr,sub,sup,strike,pre,blockquote,caption
		)
	}
	
	buttons {
		bold.hotKey = b
		italic.hotKey = i
		formatblock.removeItems = section, pre,address, h1, h6, pre, address, div, blockquote,article,footer,nav,header,aside,que
	}
	
	#hideButtons (
	#	underline, left, center, right,
	#	class, blockstylelabel, blockstyle, textstylelabel, textstyle,
	#	formatblock, bold, italic, subscript, superscript,
	#	orderedlist, unorderedlist, outdent, indent, textindicator,
	#	insertcharacter, link, table, findreplace, chMode, removeformat, undo, redo, about,
	#	toggleborders, tableproperties,
	#	rowproperties, rowinsertabove, rowinsertunder, rowdelete, rowsplit,
	#	columninsertbefore, columninsertafter, columndelete, columnsplit,
	#	cellproperties, cellinsertbefore, cellinsertafter, celldelete, cellsplit, cellmerge
	#)
	hideButtons >
	showButtons (    
		undo, redo, copy, cut, paste, bar, findreplace, removeformat, bar, chMode, linebreak,         
		formatblock, blockstyle, bar, bold, italic, bar, orderedlist, unorderedlist, bar, link, bar, 
		linebreak, table, bar, toggleborders, bar, tableproperties, bar,
		rowproperties, rowinsertabove, rowinsertunder, rowdelete, rowsplit, bar,
		columninsertbefore, columninsertafter, columndelete, columnsplit, bar, 
		cellproperties, cellinsertbefore, cellinsertafter, celldelete, cellsplit, cellmerge
	) 
	toolbarOrder (         
		undo, redo, copy, cut, paste, bar, findreplace, removeformat, bar, chMode, linebreak,         
		formatblock, blockstyle, bar, bold, italic, bar,  orderedlist, unorderedlist, bar, link, bar, 
		linebreak, table, bar, toggleborders, bar, tableproperties, bar,
		rowproperties, rowinsertabove, rowinsertunder, rowdelete, rowsplit, bar,
		columninsertbefore, columninsertafter, columndelete, columnsplit, bar, 
		cellproperties, cellinsertbefore, cellinsertafter, celldelete, cellsplit, cellmerge
	)
	
	entryHTMLparser_db = 1
	entryHTMLparser_db {
		allowTags = p,b,i,strong,em,h2,h3,h4,h5,span,div,a,br,table, thead, tbody, tr, th, td,ul,ol,li 
	}   
	
	exitHTMLparser_db = 1
	exitHTMLparser_db {
		allowTags = p,b,i,strong,em,h2,h3,h4,h5,span,div,a,br,table, thead, tbody, tr, th, td,ul,ol,li 
		tags {
			b.remap = strong
			i.remap = em
		}
	}      
	
	exitHTMLparser_rte = 1
	exitHTMLparser_rte {
		allowTags = p,b,i,strong,em,h2,h3,h4,h5,span,div,a,br,table, thead, tbody, tr, th, td,ul,ol,li
		tags {
			strong.remap = b
			em.remap = i
		}
	}
	
	
	enableWordClean = 1
	removeTrailingBR = 1
	removeComments = 1
	removeTags = center, font, o:p, sdfield, strike, u, section, pre,address, h1, h6, pre, address, div, blockquote,article,footer,nav,header,aside,que
	removeTagsAndContents = link, meta, script, style, title
	
	
	
	keepButtonGroupTogether = 1  
	keepToggleBordersInToolbar = 1 
	
	buttons.toggleborders.setOnTableCreation = 1
	
	buttons.link.popupSelector.disabled = 1
	buttons.link.targetSelector.disabled = 0
	buttons.link.options.removeItems = 1
	
	hideTableOperationsInToolbar = 0

	// Description
	disableDescriptionFieldsetInTableOperations = 1
	  // Style
	disableStyleFieldsetInTableOperations = 1
	
	// Spacing and padding
	disableSpacingFieldsetInTableOperations = 1
	// Alignment
	disableAlignmentFieldsetInTableOperations = 0
	// Background and colors
	disableColorFieldsetInTableOperations = 1
	// Layout
	disableLayoutFieldsetInTableOperations = 1
	// Frame and borders
	disableBordersFieldsetInTableOperations = 1
	  
	showStatusBar = 1
}